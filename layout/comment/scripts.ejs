<% if (theme.comments.disqus) { %>
    <%- partial('comment/disqus', { script: true }) %>
<% } else if (theme.comments.duoshuo) { %>
    <%- partial('comment/duoshuo', { script: true }) %>
<% } else if (theme.comments.youyan) { %>
    <%- partial('comment/youyan', { script: true }) %>
<% } %>